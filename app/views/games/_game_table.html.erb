<table>
    <thead>
        <tr>
            <th>Box Image</th>
            <th>Name</th>
            <% if @group %>
                <th>Group Rating</th>
            <% else %>
                <th>Your Rating</th>
                <th>Actions</th>
            <% end %>
        </tr>
    </thead>
    <tbody>
        <% @games.each do |g| %>
            <tr>
                <td><img src="<%= g[:thumb] %>"></td>
                <td><%= g[:name] %></td>
                <% if @group %>
                    <td><%= g[:group_rating] %></td>
                <% else %>
                    <td>
                        <form id="r_<%= g[:bgg_id] %>" action="<%= game_path g[:bgg_id] %>" method="post">
                            <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                            <div class="range-field">
                                <input type="range" name="rating" min="1" max="10" <% if g.include?(:user_rating)%>value="<%= g[:user_rating] %>"<% end %>>
                            </div>
                        </form>
                        <div class="form_result"></div>
                    </td>
                    <td>
                        <button class="submit_rating btn-flat tooltipped" data-tooltip="Save this rating" form-target="#r_<%= g[:bgg_id] %>"><i class="material-icons">save</i></button>
                        <% if @home %>
                            <a class="btn-flat tooltipped" href="<%= games_remove_path g[:bgg_id] %>" data-tooltip="Remove from your list" data-method="delete"><i class="material-icons">delete</i></a>
                        <% end %>
                    </td>
                <% end %>
            </tr>
        <% end %>
    </tbody>
</table>